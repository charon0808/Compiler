int fib(int a, int b)
{
    b = b + 1;
    if (a == 0)
        return 1;
    else if (a == 1)
        return 1;
    else
    {
        return fib(a - 1, 3) + fib(a - 2, b);
    }
} 

int main()
{
    int n;
    n = read();
    write(fib(n, 4));
    return 0;
}