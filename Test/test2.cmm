int fact(int n)
{
    if (n == 1)
        return n;
    else
        return (n * fact(n - 1));
}
int main()
{
    int m, result;
    m = read();
    m = m + 2;
    m = m - 2;
    if (m > 1)
        result = fact(m);
    else
        result = 1;
    write(result);
    return 0;
}